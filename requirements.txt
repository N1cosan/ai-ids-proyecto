# === Core API ===
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.9

# === ML / Detección ===
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# === Telegram + HTTP ===
httpx>=0.27.0
python-dotenv>=1.0.0

# === Utilidades ===
# (sqlite3 viene en la librería estándar de Python, no se instala)